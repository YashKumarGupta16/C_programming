#include <stdio.h>
int main()
{   for (int i=1;i<=6;i=i+1)     //i=i+1 can be written as i++
                                 //first see initialization,if it is true,obey below condition then check updation if its also true then obey again and repeat it until updation becomes false 
{
    printf("hello yash\n");
}

    return 0;
}