#include <stdio.h>
#include <string.h>
int main()
{
    char str[40];
    scanf("%s",str); //no need to use & and but scanf will only take first word as input
    printf("you typed : %s",str);
     
     
     
    return 0;
}